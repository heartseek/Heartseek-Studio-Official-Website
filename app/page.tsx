export default function Home() {
  return (
    <main className="hero">
      <div className="hero__content">
        <p className="hero__eyebrow">Next.js + TypeScript + App Router</p>
        <h1>Heartseek Studio Official Website</h1>
        <p className="hero__copy">
          A clean starter template is ready. Begin building sections, routes,
          and content from <code>app/page.tsx</code>.
        </p>
      </div>
    </main>
  );
}
